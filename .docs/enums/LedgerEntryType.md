# LedgerEntryType

## Purpose
Legacy/alternate grouping of ledger entry direction/type.

## Values
| Value | Meaning |
|---|---|
| `INBOUND` | Allowed `LedgerEntryType` value used by business rules. |
| `OUTBOUND` | Allowed `LedgerEntryType` value used by business rules. |
| `ADJUSTMENT` | Allowed `LedgerEntryType` value used by business rules. |
| `TRANSFER` | Allowed `LedgerEntryType` value used by business rules. |
