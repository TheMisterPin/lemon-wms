# AlertRuleType

## Purpose
Defines rule categories for generating alerts.

## Values
| Value | Meaning |
|---|---|
| `LOW_STOCK` | Allowed `AlertRuleType` value used by business rules. |
| `EXPIRY` | Allowed `AlertRuleType` value used by business rules. |
| `BIN_CAPACITY` | Allowed `AlertRuleType` value used by business rules. |
