# ErrorType

## Purpose
Classifies captured errors by origin/type.

## Values
| Value | Meaning |
|---|---|
| `CLIENT` | Allowed `ErrorType` value used by business rules. |
| `SERVER` | Allowed `ErrorType` value used by business rules. |
| `NETWORK` | Allowed `ErrorType` value used by business rules. |
| `UNKNOWN` | Allowed `ErrorType` value used by business rules. |
