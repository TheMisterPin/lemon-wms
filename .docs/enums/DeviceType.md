# DeviceType

## Purpose
Defines hardware/device categories in the platform.

## Values
| Value | Meaning |
|---|---|
| `FLOOR` | Allowed `DeviceType` value used by business rules. |
| `HANDHELD` | Allowed `DeviceType` value used by business rules. |
| `PC` | Allowed `DeviceType` value used by business rules. |
| `TABLET` | Allowed `DeviceType` value used by business rules. |
