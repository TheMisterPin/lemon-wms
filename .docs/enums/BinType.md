# BinType

## Purpose
Represents functional type of a bin.

## Values
| Value | Meaning |
|---|---|
| `RECEIVING` | Allowed `BinType` value used by business rules. |
| `STORAGE` | Allowed `BinType` value used by business rules. |
| `PICKING` | Allowed `BinType` value used by business rules. |
| `PACKING` | Allowed `BinType` value used by business rules. |
| `SHIPPING` | Allowed `BinType` value used by business rules. |
| `QUARANTINE` | Allowed `BinType` value used by business rules. |
| `STAGING` | Allowed `BinType` value used by business rules. |
| `CUSTOM` | Allowed `BinType` value used by business rules. |
