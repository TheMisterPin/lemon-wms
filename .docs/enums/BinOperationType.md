# BinOperationType

## Purpose
Classifies warehouse operation events performed in bins.

## Values
| Value | Meaning |
|---|---|
| `RECEIVE` | Allowed `BinOperationType` value used by business rules. |
| `PUTAWAY` | Allowed `BinOperationType` value used by business rules. |
| `MOVE` | Allowed `BinOperationType` value used by business rules. |
| `PICK` | Allowed `BinOperationType` value used by business rules. |
| `SHIP` | Allowed `BinOperationType` value used by business rules. |
| `BOX` | Allowed `BinOperationType` value used by business rules. |
| `UNBOX` | Allowed `BinOperationType` value used by business rules. |
| `ADJUST` | Allowed `BinOperationType` value used by business rules. |
| `BLOCK` | Allowed `BinOperationType` value used by business rules. |
| `UNBLOCK` | Allowed `BinOperationType` value used by business rules. |
| `COUNT` | Allowed `BinOperationType` value used by business rules. |
| `TRANSFER_LOAD` | Allowed `BinOperationType` value used by business rules. |
| `TRANSFER_UNLOAD` | Allowed `BinOperationType` value used by business rules. |
| `RETURN_RECEIVE` | Allowed `BinOperationType` value used by business rules. |
| `RETURN_PUTAWAY` | Allowed `BinOperationType` value used by business rules. |
| `REVERSE_OPERATION` | Allowed `BinOperationType` value used by business rules. |
