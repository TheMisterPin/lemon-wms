# BoxStatus

## Purpose
Represents lifecycle state of a box/container.

## Values
| Value | Meaning |
|---|---|
| `OPEN` | Allowed `BoxStatus` value used by business rules. |
| `SEALED` | Allowed `BoxStatus` value used by business rules. |
| `IN_TRANSIT` | Allowed `BoxStatus` value used by business rules. |
| `RECEIVED` | Allowed `BoxStatus` value used by business rules. |
| `SHIPPED` | Allowed `BoxStatus` value used by business rules. |
| `SCRAPPED` | Allowed `BoxStatus` value used by business rules. |
