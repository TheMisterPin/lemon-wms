# OrderPriority

## Purpose
Represents execution urgency for orders.

## Values
| Value | Meaning |
|---|---|
| `NORMAL` | Allowed `OrderPriority` value used by business rules. |
| `URGENT` | Allowed `OrderPriority` value used by business rules. |
| `EXPRESS` | Allowed `OrderPriority` value used by business rules. |
