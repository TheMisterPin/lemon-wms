# BusinessPartyType

## Purpose
Defines external party classification.

## Values
| Value | Meaning |
|---|---|
| `CUSTOMER` | Allowed `BusinessPartyType` value used by business rules. |
| `SUPPLIER` | Allowed `BusinessPartyType` value used by business rules. |
| `CARRIER` | Allowed `BusinessPartyType` value used by business rules. |
