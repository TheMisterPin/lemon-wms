# FiscalInventoryEventType

## Purpose
Classifies fiscal inventory ledger events.

## Values
| Value | Meaning |
|---|---|
| `RECEIPT` | Allowed `FiscalInventoryEventType` value used by business rules. |
| `SHIPMENT` | Allowed `FiscalInventoryEventType` value used by business rules. |
| `RETURN_IN` | Allowed `FiscalInventoryEventType` value used by business rules. |
| `RETURN_OUT` | Allowed `FiscalInventoryEventType` value used by business rules. |
| `ADJUSTMENT_IN` | Allowed `FiscalInventoryEventType` value used by business rules. |
| `ADJUSTMENT_OUT` | Allowed `FiscalInventoryEventType` value used by business rules. |
| `TRANSFER_IN` | Allowed `FiscalInventoryEventType` value used by business rules. |
| `TRANSFER_OUT` | Allowed `FiscalInventoryEventType` value used by business rules. |
