---
phase: 21-primitive-and-styling-plan
plan: 3
type: execute
wave: 2
depends_on:
  - 21-01-PLAN.md
files_modified:
  - .docs/developer/refactors/components/component/dashboard/warehouses/components/overview-cards.md
  - .docs/developer/refactors/components/component/dashboard/warehouses/components/directory-sections.md
  - .docs/developer/refactors/_primitive-extraction-plan.md
autonomous: true
requirements:
  - CFR-14
  - CFR-15
  - CFR-17
must_haves:
  truths:
    - Each targeted refactor doc gains an explicit **Primitive candidate specification (Phase 21 / CFR-14)** subsection OR a clear **Reject / defer** rationale tied to CFR-15.
    - Master extraction plan gains one row per evaluated nested pattern (`StatCard`, section/list shells).
    - No `src/**` edits.
  artifacts:
    - path: .docs/developer/refactors/components/component/dashboard/warehouses/components/overview-cards.md
      provides: StatCard primitive evaluation
      contains: Primitive candidate specification
    - path: .docs/developer/refactors/components/component/dashboard/warehouses/components/directory-sections.md
      provides: Section/list shell evaluation
      contains: Primitive candidate specification
---

<objective>
Evaluate **nested presentational patterns** in `OverviewCards` and `DirectorySections` for primitive eligibility: repeated domain-neutral structure vs locations-specific coupling.

Document CFR-14 fields **per evaluated sub-component** (StatCard; SectionBlock/ListCard or equivalent nested shells named in Phase 19 Logic Mapping). Default stance from `21-RESEARCH.md`: likely **defer** until second cross-feature consumer is proven — document evidence either way.

Append rows to `_primitive-extraction-plan.md` (IDs **P21-002**, **P21-003**, etc.).
</objective>

<execution_context>
@/Users/michele/Dev/Projects/NextJS/lemon-wms/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-primitive-and-styling-plan/21-CONTEXT.md
@.planning/phases/21-primitive-and-styling-plan/21-RESEARCH.md
@.docs/developer/refactors/components/component/dashboard/warehouses/components/overview-cards.md
@.docs/developer/refactors/components/component/dashboard/warehouses/components/directory-sections.md
@.docs/developer/refactors/_primitive-extraction-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OverviewCards — StatCard CFR-14 evaluation</name>
  <read_first>
    - `.docs/developer/refactors/components/component/dashboard/warehouses/components/overview-cards.md`
    - Source file `src/components/dashboard/warehouses/components/OverviewCards.tsx` if needed for accurate prop sketch (read-only; do not modify)
  </read_first>
  <files>.docs/developer/refactors/components/component/dashboard/warehouses/components/overview-cards.md</files>
  <action>
    Add **`## Primitive candidate specification (Phase 21 / CFR-14)`** for **`StatCard`** (nested implementation): purpose; whether consumers exist outside this module (grep-based evidence or explicit "single parent only"); target primitive path **if approve** (e.g. `src/components/primitives/dashboard-stat-card.tsx` — naming planner-owned); props sketch; token/styling notes; allowed/forbidden; **Recommendation** **`defer`** unless grep proves ≥2 independent feature modules import an extracted StatCard-shaped shell — document grep command used in Notes.

    If nested components besides StatCard qualify for separate bullets, add **Additional nested evaluations** sub-subsection with terse rows (still CFR-14-shaped).
  </action>
  <verify>
    <automated>rg -n 'Primitive candidate specification \(Phase 21 / CFR-14\)|StatCard|defer|CFR-15' .docs/developer/refactors/components/component/dashboard/warehouses/components/overview-cards.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    StatCard evaluation explicit; recommendation justified with consumer evidence; src untouched.
  </acceptance_criteria>
  <done>OverviewCards primitive story documented.</done>
</task>

<task type="auto">
  <name>Task 2: DirectorySections — section/list shells CFR-14 evaluation</name>
  <read_first>
    - `.docs/developer/refactors/components/component/dashboard/warehouses/components/directory-sections.md`
    - Source `src/components/dashboard/warehouses/components/DirectorySections.tsx` read-only as needed
  </read_first>
  <files>.docs/developer/refactors/components/component/dashboard/warehouses/components/directory-sections.md</files>
  <action>
    Add **`## Primitive candidate specification (Phase 21 / CFR-14)`** covering **`SectionBlock`** / **`ListCard`** (use actual nested component names from source). Same structure as Task 1. Likely **`defer`** due to locations directory semantics — if **reject** due to domain copy or href rules, cite CFR-15 domain-rule clause in Forbidden responsibilities.

    Capture **skeleton / loading** coupling per CFR-17 if relevant (probably none — reference sibling skeleton docs only).
  </action>
  <verify>
    <automated>rg -n 'Primitive candidate specification \(Phase 21 / CFR-14\)|SectionBlock|ListCard|defer|reject' .docs/developer/refactors/components/component/dashboard/warehouses/components/directory-sections.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Nested shells evaluated; recommendation explicit; src untouched.
  </acceptance_criteria>
  <done>DirectorySections primitive story documented.</done>
</task>

<task type="auto">
  <name>Task 3: Master plan rows for overview + directory candidates</name>
  <read_first>
    - `.docs/developer/refactors/_primitive-extraction-plan.md`
    - Updated overview-cards and directory-sections sections
  </read_first>
  <files>.docs/developer/refactors/_primitive-extraction-plan.md</files>
  <action>
    Append rows **P21-002** (StatCard / overview-cards), **P21-003** (directory section shells — split into two rows **P21-003a** / **P21-003b** if two distinct primitives evaluated). Columns filled consistently with Task 1–2 recommendations; Notes link back to markdown headings.
  </action>
  <verify>
    <automated>rg -n 'P21-002|P21-003' .docs/developer/refactors/_primitive-extraction-plan.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Rows exist; IDs unique; recommendations align with docs.
  </acceptance_criteria>
  <done>Master register updated for locations overview slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → repo | No `src/**` modifications — source reads only for accuracy. |
</threat_model>

<verification>
`rg 'P21-002|P21-003' .docs/developer/refactors/_primitive-extraction-plan.md`; empty src diff.
</verification>

<success_criteria>
Overview + directory primitive evaluations captured; master rows appended.
</success_criteria>

<output>
After completion, create `.planning/phases/21-primitive-and-styling-plan/21-03-SUMMARY.md` per execute workflow.
</output>
