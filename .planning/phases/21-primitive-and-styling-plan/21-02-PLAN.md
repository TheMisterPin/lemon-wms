---
phase: 21-primitive-and-styling-plan
plan: 2
type: execute
wave: 2
depends_on:
  - 21-01-PLAN.md
files_modified:
  - .docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-primitives.md
  - .docs/developer/refactors/_primitive-extraction-plan.md
autonomous: true
requirements:
  - CFR-14
  - CFR-15
  - CFR-16
must_haves:
  truths:
    - `warehouse-overview-primitives.md` contains a complete CFR-14 primitive candidate specification section.
    - Master extraction plan includes a row for this module with recommendation and consumer evidence pointer.
    - No `src/**` edits.
  artifacts:
    - path: .docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-primitives.md
      provides: CFR-14 primitive specification for flagship cluster
      contains: Primitive candidate specification
    - path: .docs/developer/refactors/_primitive-extraction-plan.md
      provides: Row P21-warehouse-overview-primitives
      contains: warehouse-overview-primitives
---

<objective>
Finalize **CFR-14 documentation** for the only row classified **primitive** in Phase 19 (`warehouse-overview-primitives.tsx`): purpose, sources, target path, props sketch, styling/token rules, allowed vs forbidden responsibilities, migration usage sketch, open questions — without implementing code.

Append the corresponding **master row** to `_primitive-extraction-plan.md`.

Purpose: Satisfies ROADMAP Phase 21 success criteria #1 and #3 for the highest-signal primitive cluster.
</objective>

<execution_context>
@/Users/michele/Dev/Projects/NextJS/lemon-wms/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-primitive-and-styling-plan/21-CONTEXT.md
@.docs/developer/refactors/components/_inventory-summary.md
@.docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-primitives.md
@.docs/developer/refactors/_primitive-extraction-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CFR-14 section in warehouse-overview-primitives doc</name>
  <read_first>
    - `.docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-primitives.md`
    - `.docs/developer/refactors/components/_inventory-summary.md` (consumer row for warehouse-overview-primitives)
    - `.planning/phases/21-primitive-and-styling-plan/21-CONTEXT.md`
  </read_first>
  <files>.docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-primitives.md</files>
  <action>
    Append section **`## Primitive candidate specification (Phase 21 / CFR-14)`** after the existing `## Logic Mapping` block (or nearest logical anchor — preserve prior headings). Include subsections: **Purpose**, **Source module**, **Consumers** (list dashboard files from `_inventory-summary.md` row or equivalent grep of imports — must name ≥3 consuming dashboards/pages), **Target path** (`src/components/primitives/warehouse-overview-primitives.tsx` per Phase 19), **Props / exports sketch** (bullet list per exported presentational component — no large pasted source), **Styling & tokens** (`--wh-*` dependence explicit per `21-RESEARCH`), **Allowed responsibilities**, **Forbidden responsibilities** (mirror CFR-15), **Migration usage sketch** (how feature pages import-only presentational exports in Phase 22), **Open questions** (e.g. inline type split to `src/types/dto` — Phase 22, not Phase 21 behavior change). State CFR-16: **no visual change** during extraction.

    Recommendation line at end of section: default **`approve`** for Phase 22 primitive extraction **only if** CFR-15 disqualifiers are absent — document explicit sentence that hooks/API clients must not appear in this module today (verify from source read if needed — docs-only citation).
  </action>
  <verify>
    <automated>rg -n 'Primitive candidate specification \(Phase 21 / CFR-14\)|CFR-15|approve|--wh-' .docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-primitives.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Section exists with all named subsections; consumers enumerated; `git diff --name-only -- 'src/**'` empty.
  </acceptance_criteria>
  <done>Flagship primitive fully specified for Phase 22.</done>
</task>

<task type="auto">
  <name>Task 2: Master plan row P21-warehouse-overview-primitives</name>
  <read_first>
    - `.docs/developer/refactors/_primitive-extraction-plan.md`
    - `.docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-primitives.md` (new CFR-14 section)
  </read_first>
  <files>.docs/developer/refactors/_primitive-extraction-plan.md</files>
  <action>
    Append one data row to the master table: `ID` **P21-001**, candidate **`warehouse-overview-primitives`**, source doc path, source code path `src/components/dashboard/warehouses/components/warehouse-overview-primitives.tsx`, evidence summary ("multi-dashboard KPI/chart/shell exports; `--wh-*` shells"), target path `src/components/primitives/warehouse-overview-primitives.tsx`, recommendation **`approve`** (or **`defer`** only if source review finds CFR-15 violation — if defer, document blocker in Notes), risk **high**, Notes pointing to CFR-14 section anchor.
  </action>
  <verify>
    <automated>rg -n 'P21-001|warehouse-overview-primitives' .docs/developer/refactors/_primitive-extraction-plan.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Row present; recommendation coherent with doc section; no src edits.
  </acceptance_criteria>
  <done>Aggregation table reflects flagship primitive decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → repo | No runtime edits under `src/**`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | I | CFR-14 doc | mitigate | Consumer list must match inventory or import grep evidence cited in Notes. |
</threat_model>

<verification>
`rg 'P21-001' .docs/developer/refactors/_primitive-extraction-plan.md`; empty src diff.
</verification>

<success_criteria>
warehouse-overview-primitives documented to CFR-14; master row appended.
</success_criteria>

<output>
After completion, create `.planning/phases/21-primitive-and-styling-plan/21-02-SUMMARY.md` per execute workflow.
</output>
