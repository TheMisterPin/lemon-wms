---
phase: 21-primitive-and-styling-plan
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .docs/developer/refactors/_primitive-extraction-plan.md
  - .docs/developer/refactors/components/_classification-summary.md
autonomous: true
requirements:
  - CFR-14
  - CFR-15
  - CFR-16
  - CFR-17
must_haves:
  truths:
    - Phase 21 executor creates the aggregation artifact `_primitive-extraction-plan.md` with a stable table schema and Phase 22 gate language.
    - Classification summary points readers at the aggregation doc for Phase 21 outcomes.
    - Refactor tree remains free of `src/**` edits after this plan.
  artifacts:
    - path: .docs/developer/refactors/_primitive-extraction-plan.md
      provides: Master primitive candidate register for Phase 21–22
      contains: Primitive extraction plan
    - path: .docs/developer/refactors/components/_classification-summary.md
      provides: Cross-link to aggregation doc
      contains: _primitive-extraction-plan
  key_links:
    - from: _classification-summary.md
      to: _primitive-extraction-plan.md
      via: Unresolved follow-ups / Phase 21 bullet
---

<objective>
Create `.docs/developer/refactors/_primitive-extraction-plan.md` as the **single aggregation table** ROADMAP Phase 21 success criterion #2 requires, plus a short **how to read** section aligned with CFR-14–CFR-17 and `21-CONTEXT.md` decisions D-21-01–D-21-11.

Later plans (21-02+) append concrete rows; this plan establishes headers, approval vocabulary (**approve** / **defer** / **reject**), and evidence rules without guessing outcomes.

Output: new file under `.docs/developer/refactors/` and a minimal pointer edit in `_classification-summary.md`.
</objective>

<execution_context>
@/Users/michele/Dev/Projects/NextJS/lemon-wms/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-primitive-and-styling-plan/21-CONTEXT.md
@.planning/phases/21-primitive-and-styling-plan/21-RESEARCH.md
@.cursor/rules/styling-and-primitives.mdc
@.docs/developer/refactors/components/_classification-summary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `_primitive-extraction-plan.md` scaffold</name>
  <read_first>
    - `.planning/phases/21-primitive-and-styling-plan/21-CONTEXT.md`
    - `.planning/phases/21-primitive-and-styling-plan/21-RESEARCH.md`
    - `.cursor/rules/styling-and-primitives.mdc`
  </read_first>
  <files>.docs/developer/refactors/_primitive-extraction-plan.md</files>
  <action>
    Create `.docs/developer/refactors/_primitive-extraction-plan.md` with: (1) **Purpose** — Phase 21 documentation-only; Phase 22 executes moves only where this plan marks **approve** or documents **defer** rationale; CFR-16 visual preservation gate. (2) **CFR-15 disqualifiers** bullet list (no fetch/mutation/feature hooks/API shapes/domain rules). (3) **Master table** with columns: `ID` (P21-xxx), `Candidate name`, `Source doc path`, `Source code path`, `Repeated pattern evidence (short)`, `Target primitive path (if approve)`, `Recommendation`, `Risk`, `Notes / open questions`. (4) One example row stub explaining convention — may use `warehouse-overview-primitives` as **to be filled in 21-02** without committing approve/defer yet **or** leave zero data rows and state "rows added by waves 21-02–21-05". Prefer **zero data rows** plus explicit sentence that waves follow. (5) **Skeleton placement annex** subsection header for CFR-17 (empty table with columns `Skeleton cluster doc`, `Placement decision`, `Rationale`) — filled starting 21-04. (6) Link to `.cursor/rules/styling-and-primitives.mdc` and `21-CONTEXT.md` path under `.planning/`.
  </action>
  <verify>
    <automated>test -f .docs/developer/refactors/_primitive-extraction-plan.md; rg -n 'CFR-15|CFR-17|Recommendation|Primitive extraction plan' .docs/developer/refactors/_primitive-extraction-plan.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Scaffold file exists; master table column headers present; disqualifiers explicit; `git diff --name-only -- 'src/**'` empty.
  </acceptance_criteria>
  <done>Aggregation artifact exists; ready for row append in later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-link from classification summary</name>
  <read_first>
    - `.docs/developer/refactors/components/_classification-summary.md` (Unresolved follow-ups)
    - `.docs/developer/refactors/_primitive-extraction-plan.md`
  </read_first>
  <files>.docs/developer/refactors/components/_classification-summary.md</files>
  <action>
    In `## Unresolved follow-ups`, replace or augment the Phase 21 bullet so it explicitly references `.docs/developer/refactors/_primitive-extraction-plan.md` as the **canonical Phase 21 output** for approve/defer/reject with reuse evidence. Do not remove Phase 22/23 bullets; keep tone consistent with existing list.
  </action>
  <verify>
    <automated>rg -n '_primitive-extraction-plan\.md' .docs/developer/refactors/components/_classification-summary.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Classification summary mentions the aggregation file by path; no `src/**` changes.
  </acceptance_criteria>
  <done>Readers can navigate from classification summary to extraction plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → repo | Markdown edits must never alter runtime code paths under `src/**`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | T | PLAN executor | mitigate | Task verify: empty `src/**` diff. |
</threat_model>

<verification>
After wave: `test -f .docs/developer/refactors/_primitive-extraction-plan.md`; `git diff --name-only -- 'src/**'` empty.
</verification>

<success_criteria>
Scaffold + cross-link complete; src tree untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/21-primitive-and-styling-plan/21-01-SUMMARY.md` per execute workflow.
</output>
