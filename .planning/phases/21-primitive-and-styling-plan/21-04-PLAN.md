---
phase: 21-primitive-and-styling-plan
plan: 4
type: execute
wave: 3
depends_on:
  - 21-01-PLAN.md
files_modified:
  - .docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-dashboard-overview-skeleton.md
  - .docs/developer/refactors/components/component/dashboard/warehouses/dashboard-location-page-skeleton.md
  - .docs/developer/refactors/components/component/dashboard/stock/dashboard-stock-page-skeleton.md
  - .docs/developer/refactors/components/component/dashboard/zones/components/zone-overview-dashboard-skeleton.md
  - .docs/developer/refactors/_primitive-extraction-plan.md
autonomous: true
requirements:
  - CFR-17
  - CFR-14
  - CFR-16
must_haves:
  truths:
    - Each skeleton cluster doc records **placement decision** (feature-local vs primitive candidate) consistent with CFR-17.
    - `_primitive-extraction-plan.md` **Skeleton placement annex** populated for these four clusters.
    - No `src/**` edits.
  artifacts:
    - path: .docs/developer/refactors/_primitive-extraction-plan.md
      provides: Skeleton annex rows
      contains: Skeleton cluster doc
---

<objective>
Resolve **skeleton placement** (CFR-17) for the four highest-documented dashboard skeleton clusters: warehouse overview, locations page, stock dashboard, zone overview.

Default per `21-CONTEXT.md` D-21-07 / D-21-08: **feature-local clusters stay grouped**; generic shimmer/KPI tiles become primitive candidates **only** if reuse is proven across modules — document **defer** with explicit cross-links to dismounted shimmer/KPI docs where Phase 19 already listed them.

Populate the **Skeleton placement annex** table introduced in `_primitive-extraction-plan.md` during 21-01.

Append master-table rows only if a **standalone generic primitive** is approved (unlikely here); otherwise annex-only placement decisions suffice.
</objective>

<execution_context>
@/Users/michele/Dev/Projects/NextJS/lemon-wms/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-primitive-and-styling-plan/21-CONTEXT.md
@.docs/developer/refactors/_primitive-extraction-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Warehouse + locations skeleton clusters — CFR-17 decisions</name>
  <read_first>
    - `.docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-dashboard-overview-skeleton.md`
    - `.docs/developer/refactors/components/component/dashboard/warehouses/dashboard-location-page-skeleton.md`
  </read_first>
  <files>
    .docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-dashboard-overview-skeleton.md
    .docs/developer/refactors/components/component/dashboard/warehouses/dashboard-location-page-skeleton.md
  </files>
  <action>
    Append subsection **`## Skeleton placement (Phase 21 / CFR-17)`** to each file with: **Decision** (`feature-local grouped` vs `defer generic primitive` vs `reject mega-primitive`); **Rationale** referencing repeated layout vs domain-specific copy; **Related dismounted docs** (Shimmer, KPI tiles, etc. — paths under `.docs/developer/refactors/components/dismounted/`); **Phase 22 note** (extract inner pieces only if readability demands — still no visual change).

    Expected default: **feature-local grouped** for both clusters; generic KPI/shimmer extraction **defer** pending byte-level comparison across locations vs stock clusters (cite as open follow-up, not Phase 21 implementation).
  </action>
  <verify>
    <automated>rg -n 'Skeleton placement \(Phase 21 / CFR-17\)|feature-local|defer' .docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-dashboard-overview-skeleton.md .docs/developer/refactors/components/component/dashboard/warehouses/dashboard-location-page-skeleton.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Both docs carry explicit CFR-17 subsection; src untouched.
  </acceptance_criteria>
  <done>Warehouse/locations skeleton placement locked in docs.</done>
</task>

<task type="auto">
  <name>Task 2: Stock + zone skeleton clusters — CFR-17 decisions</name>
  <read_first>
    - `.docs/developer/refactors/components/component/dashboard/stock/dashboard-stock-page-skeleton.md`
    - `.docs/developer/refactors/components/component/dashboard/zones/components/zone-overview-dashboard-skeleton.md`
  </read_first>
  <files>
    .docs/developer/refactors/components/component/dashboard/stock/dashboard-stock-page-skeleton.md
    .docs/developer/refactors/components/component/dashboard/zones/components/zone-overview-dashboard-skeleton.md
  </files>
  <action>
    Same **`## Skeleton placement (Phase 21 / CFR-17)`** pattern as Task 1. Call out stock-vs-locations shimmer duplication hypothesis — **defer** generic primitive unless executor confirms matching structure via read-only source comparison (document outcome in Notes).

    Zone overview skeleton should remain **feature-local** unless KPI strip reuse crosses stock/locations without domain wording — default conservative.
  </action>
  <verify>
    <automated>rg -n 'Skeleton placement \(Phase 21 / CFR-17\)' .docs/developer/refactors/components/component/dashboard/stock/dashboard-stock-page-skeleton.md .docs/developer/refactors/components/component/dashboard/zones/components/zone-overview-dashboard-skeleton.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Both docs updated; decisions explicit; src untouched.
  </acceptance_criteria>
  <done>Stock/zone skeleton placement locked in docs.</done>
</task>

<task type="auto">
  <name>Task 3: Populate skeleton annex in `_primitive-extraction-plan.md`</name>
  <read_first>
    - `.docs/developer/refactors/_primitive-extraction-plan.md`
    - The four skeleton markdown files after Tasks 1–2
  </read_first>
  <files>.docs/developer/refactors/_primitive-extraction-plan.md</files>
  <action>
    Fill **Skeleton placement annex** table with four rows — one per cluster doc path — columns match scaffold from 21-01 (`Skeleton cluster doc`, `Placement decision`, `Rationale`). Summaries must agree with each file’s CFR-17 subsection.

    Optionally add master-table rows **P21-0xx** only if Task 1–2 elevated a **generic** shimmer/KPI primitive to **approve** (unlikely); if none, add a short **No standalone skeleton primitives approved** bullet under annex.
  </action>
  <verify>
    <automated>rg -n 'Skeleton placement annex|warehouse-dashboard-overview-skeleton|dashboard-stock-page-skeleton|zone-overview-dashboard-skeleton|dashboard-location-page-skeleton' .docs/developer/refactors/_primitive-extraction-plan.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Annex has four cluster rows; coherent with per-doc sections.
  </acceptance_criteria>
  <done>Skeleton register synchronized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → repo | No `src/**` edits. |
</threat_model>

<verification>
Four skeleton docs contain `Skeleton placement (Phase 21 / CFR-17)`; annex lists four paths.
</verification>

<success_criteria>
CFR-17 placement documented cluster-by-cluster and summarized in extraction plan annex.
</success_criteria>

<output>
After completion, create `.planning/phases/21-primitive-and-styling-plan/21-04-SUMMARY.md` per execute workflow.
</output>
