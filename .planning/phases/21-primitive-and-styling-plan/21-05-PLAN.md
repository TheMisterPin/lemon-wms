---
phase: 21-primitive-and-styling-plan
plan: 5
type: execute
wave: 4
depends_on:
  - 21-01-PLAN.md
  - 21-02-PLAN.md
  - 21-03-PLAN.md
  - 21-04-PLAN.md
files_modified:
  - .docs/developer/refactors/components/component/dashboard/stock/category-stock-page-client.md
  - .docs/developer/refactors/components/component/dashboard/bins/bin-overview-dashboard.md
  - .docs/developer/refactors/components/component/dashboard/warehouses/components/bin-grid.md
  - .docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-icons.md
  - .docs/developer/refactors/_primitive-extraction-plan.md
  - .docs/developer/refactors/_logic-mapping-summary.md
autonomous: true
requirements:
  - CFR-14
  - CFR-15
  - CFR-16
  - CFR-17
must_haves:
  truths:
    - Chart shells / pagination chrome / icon glyph utility evaluated with explicit approve/defer/reject rows in `_primitive-extraction-plan.md`.
    - `_logic-mapping-summary.md` references `_primitive-extraction-plan.md` once for Phase 21 handoff.
    - Extraction plan ends with **Completeness checklist** tied to ROADMAP Phase 21 success criteria #1–4.
    - No `src/**` edits.
---

<objective>
Close remaining **high-visibility deferrals** from Phase 19–20 narratives: stock category chart shells, bin overview chart wrappers, bin grid pagination chrome, warehouse overview icon utility map.

Finish `_primitive-extraction-plan.md` with a **Completeness checklist** proving ROADMAP criteria coverage (cite evidence paths).

Cross-link from `_logic-mapping-summary.md` so Phase 22 readers discover the primitive master plan from the Logic Mapping summary.

</objective>

<execution_context>
@/Users/michele/Dev/Projects/NextJS/lemon-wms/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-primitive-and-styling-plan/21-CONTEXT.md
@.docs/developer/refactors/_primitive-extraction-plan.md
@.docs/developer/refactors/_logic-mapping-summary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Category stock client — chart shell CFR-14 evaluation</name>
  <read_first>
    - `.docs/developer/refactors/components/component/dashboard/stock/category-stock-page-client.md`
  </read_first>
  <files>.docs/developer/refactors/components/component/dashboard/stock/category-stock-page-client.md</files>
  <action>
    Append **`## Primitive candidate specification (Phase 21 / CFR-14)`** focused on **chart panel / recharts shell** duplication vs `warehouse-overview-primitives` chart wrapper — recommendation typically **`defer`** until Phase 22 compares class lists side-by-side with `WarehouseOverviewChartPanel` usage; forbid extraction if chart config or stock DTO shaping leaks into primitive.

    Reference CFR-16 — no visual drift when consolidating later.
  </action>
  <verify>
    <automated>rg -n 'Primitive candidate specification \(Phase 21 / CFR-14\)|chart|defer|WarehouseOverviewChartPanel' .docs/developer/refactors/components/component/dashboard/stock/category-stock-page-client.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Chart primitive question resolved in prose; src untouched.
  </acceptance_criteria>
  <done>Stock chart deferral documented.</done>
</task>

<task type="auto">
  <name>Task 2: Bin overview dashboard — chart/card shell evaluation</name>
  <read_first>
    - `.docs/developer/refactors/components/component/dashboard/bins/bin-overview-dashboard.md`
  </read_first>
  <files>.docs/developer/refactors/components/component/dashboard/bins/bin-overview-dashboard.md</files>
  <action>
    Append **`## Primitive candidate specification (Phase 21 / CFR-14)`** for **Recharts layout / KPI card shells** echoing warehouse primitives — recommendation **`defer`** (same consolidation gate as stock); note shared imports from `warehouse-overview-primitives` as reuse evidence **for styling** but not automatic primitive merge (avoid mega-primitive spanning domains).
  </action>
  <verify>
    <automated>rg -n 'Primitive candidate specification \(Phase 21 / CFR-14\)|defer|Recharts|warehouse-overview-primitives' .docs/developer/refactors/components/component/dashboard/bins/bin-overview-dashboard.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Evaluation explicit; CFR-15 mega-primitive caution stated.
  </acceptance_criteria>
  <done>Bin overview chart deferral documented.</done>
</task>

<task type="auto">
  <name>Task 3: Bin grid — pagination chrome evaluation</name>
  <read_first>
    - `.docs/developer/refactors/components/component/dashboard/warehouses/components/bin-grid.md`
  </read_first>
  <files>.docs/developer/refactors/components/component/dashboard/warehouses/components/bin-grid.md</files>
  <action>
    Append **`## Primitive candidate specification (Phase 21 / CFR-14)`** for **pagination UI chrome** if called out in Phase 19 — recommendation **`defer`** until second table consumer shares identical chrome (GenericTable work paused — cite REQUIREMENTS out-of-scope).

    If pagination is tightly coupled to bin-specific labels, use **`reject`** for primitive with rationale (domain copy).
  </action>
  <verify>
    <automated>rg -n 'Primitive candidate specification \(Phase 21 / CFR-14\)|pagination|defer|reject' .docs/developer/refactors/components/component/dashboard/warehouses/components/bin-grid.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Pagination primitive stance documented.
  </acceptance_criteria>
  <done>Bin grid chrome decision logged.</done>
</task>

<task type="auto">
  <name>Task 4: Warehouse overview icons — utility vs primitive</name>
  <read_first>
    - `.docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-icons.md`
  </read_first>
  <files>.docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-icons.md</files>
  <action>
    Append **`## Primitive / utility boundary (Phase 21 / CFR-14 / CFR-15)`** concluding **`reject`** as **primitive** (glyph map + domain-ish semantics) — stays **`utility`** / feature-adjacent per Phase 19 classification; Phase 22 moves file without promoting to `components/primitives`.

    Document **Forbidden**: presenting this map as a neutral primitive — violates CFR-15 domain-neutral rule if naming/status semantics encode warehouse vocabulary.
  </action>
  <verify>
    <automated>rg -n 'Primitive / utility boundary|reject|utility|CFR-15' .docs/developer/refactors/components/component/dashboard/warehouses/components/warehouse-overview-icons.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Explicit reject-as-primitive + retain utility classification.
  </acceptance_criteria>
  <done>Icon map boundary locked.</done>
</task>

<task type="auto">
  <name>Task 5: Finalize `_primitive-extraction-plan.md` rows + completeness checklist</name>
  <read_first>
    - `.docs/developer/refactors/_primitive-extraction-plan.md`
    - Tasks 1–4 updated docs
  </read_first>
  <files>.docs/developer/refactors/_primitive-extraction-plan.md</files>
  <action>
    Append master-table rows **P21-004** through **P21-007** (adjust numbering if conflicts — maintain uniqueness) for: category-stock chart shell, bin-overview chart shells, bin-grid pagination chrome, warehouse-overview-icons primitive rejection (recommendation **`reject`** with utility retention note).

    Close file with **`## Phase 21 completeness (vs ROADMAP)`** checklist mapping: success criterion #1 → enumerate docs carrying CFR-14 sections; #2 → master table + annex; #3 → cite CFR-15 disqualifiers section; #4 → cite skeleton annex + four skeleton docs.

    Ensure **Status footer**: `Phase 21 planning inputs complete — execution pending $gsd-execute-phase 21`.
  </action>
  <verify>
    <automated>rg -n 'P21-00|Completeness|CFR-17|CFR-14' .docs/developer/refactors/_primitive-extraction-plan.md | head -n 40; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Rows + checklist present; roadmap mapping explicit; src untouched.
  </acceptance_criteria>
  <done>Master plan finalized for Phase 22.</done>
</task>

<task type="auto">
  <name>Task 6: Cross-link `_logic-mapping-summary.md`</name>
  <read_first>
    - `.docs/developer/refactors/_logic-mapping-summary.md`
  </read_first>
  <files>.docs/developer/refactors/_logic-mapping-summary.md</files>
  <action>
    Add a short bullet or subsection under an appropriate heading (e.g. after Phase 20 boundary paragraph or near **deferred** vocabulary table) pointing to `.docs/developer/refactors/_primitive-extraction-plan.md` as **canonical Phase 21 primitive decisions**. Keep edit minimal — one paragraph max.
  </action>
  <verify>
    <automated>rg -n '_primitive-extraction-plan\.md' .docs/developer/refactors/_logic-mapping-summary.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Summary links to extraction plan; src untouched.
  </acceptance_criteria>
  <done>Phase 20 summary bridges to Phase 21 artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → repo | No `src/**` edits. |
</threat_model>

<verification>
`rg '_primitive-extraction-plan\.md' .docs/developer/refactors/_logic-mapping-summary.md`; `rg 'Completeness|Phase 21 completeness' .docs/developer/refactors/_primitive-extraction-plan.md`; empty src diff.
</verification>

<success_criteria>
Deferrals closed in prose; master rows complete; checklist maps ROADMAP; logic-mapping summary cross-linked.
</success_criteria>

<output>
After completion, create `.planning/phases/21-primitive-and-styling-plan/21-05-SUMMARY.md` per execute workflow.
</output>
