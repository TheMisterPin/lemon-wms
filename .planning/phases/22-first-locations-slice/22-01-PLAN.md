---
phase: 22-first-locations-slice
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/primitives/warehouse-overview-primitives.tsx
  - src/components/dashboard/**/*.tsx
  - .docs/developer/refactors/**/*.md
autonomous: true
requirements:
  - CFR-18
  - CFR-16
must_haves:
  truths:
    - `warehouse-overview-primitives` lives at `src/components/primitives/warehouse-overview-primitives.tsx`.
    - All prior import sites resolve via `@/components/primitives/warehouse-overview-primitives`.
    - Refactor inventory/map/classification rows cite the new source path.
---

<objective>

Implement **P21-001** (Phase 21 approval): physically move **`warehouse-overview-primitives.tsx`** into **`src/components/primitives/`**, update **all** TSX imports, sync refactor markdown paths. Zero visual or behavioral change.

</objective>

<tasks>

<task type="auto">
  <name>Move module + rewire imports</name>
  <action>`git mv` from `dashboard/warehouses/components/` to `components/primitives/`; replace `@/components/dashboard/warehouses/components/warehouse-overview-primitives`, `./warehouse-overview-primitives`, and `./components/warehouse-overview-primitives` imports across `src/**/*.tsx`.</action>
</task>

<task type="auto">
  <name>Docs sync</name>
  <action>Update `warehouse-overview-primitives.md` frontmatter and paths; `_refactor-map.md`, `_classification-summary.md`, `_inventory-summary.md`, `_primitive-extraction-plan.md` P21-001 row.</action>
</task>

</tasks>

<success_criteria>Imports build at module resolution level; docs agree with tree; commit records move.</success_criteria>
