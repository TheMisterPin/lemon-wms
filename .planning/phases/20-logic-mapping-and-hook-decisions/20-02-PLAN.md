---
phase: 20-logic-mapping-and-hook-decisions
plan: 2
type: execute
wave: 2
depends_on:
  - '20-01'
files_modified:
  - .docs/developer/refactors/components/hook/dashboard/warehouses/use-dashboard-warehouse.md
  - .docs/developer/refactors/hooks/dashboard/warehouses/use-dashboard-warehouse.md
  - .docs/developer/refactors/components/_refactor-map.md
autonomous: true
requirements:
  - CFR-09
  - CFR-10
  - CFR-11
  - CFR-12
  - CFR-13
must_haves:
  truths:
    - Both warehouse hook docs contain implementation-ready tables for types/api, types/dto, transformers/locations, hook actions, provider compatibility.
    - Mutation error parsing maps to shared utility path `src/lib/api/extract-mutation-error.ts` (utility tier), not `src/lib/transformers`; devices duplication cited per CFR-13 evidence requirement.
    - Target hook documents grouped `actions` for Phase 22 when callbacks exceed two (D-20-09).
  artifacts:
    - path: .docs/developer/refactors/hooks/dashboard/warehouses/use-dashboard-warehouse.md
      provides: Canonical hook responsibility + Logic Mapping alignment
      contains: Hook Responsibility
    - path: .docs/developer/refactors/components/hook/dashboard/warehouses/use-dashboard-warehouse.md
      provides: Generated-cluster parity with canonical doc
      contains: Logic Mapping
  key_links:
    - from: warehouse hook docs
      to: src/lib/api/extract-mutation-error.ts
      via: Movement Plan row (planned Phase 22 path)
---

<objective>
Produce the implementation-ready warehouse provider/hook documentation cluster covering API types, DTOs, transformers, mutation utilities, `actions` shape, and provider compatibility checklist per D-20-04..D-20-10 and CFR-11..CFR-13.

Purpose: Phase 22 anchor for `use-dashboard-warehouse.tsx` split without reinterpretation.

Output: Updated canonical + component-hook markdown and `_refactor-map.md` integration cross-link.
</objective>

<execution_context>
@/Users/michele/Dev/Projects/NextJS/lemon-wms/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/20-logic-mapping-and-hook-decisions/20-CONTEXT.md
@.planning/phases/20-logic-mapping-and-hook-decisions/20-RESEARCH.md
@.cursor/rules/refactor-documentation-workflow.mdc
@.cursor/rules/hooks-and-data-flow.mdc
@src/components/dashboard/warehouses/use-dashboard-warehouse.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonical warehouse hook doc — Logic Mapping + CFR-11/12/13</name>
  <read_first>
    - `.docs/developer/refactors/hooks/dashboard/warehouses/use-dashboard-warehouse.md`
    - `.planning/phases/20-logic-mapping-and-hook-decisions/20-RESEARCH.md` (Q3 split tables)
    - `.cursor/rules/refactor-documentation-workflow.mdc` (Logic Mapping + Hook template)
    - `.cursor/rules/hooks-and-data-flow.mdc` (actions grouping, types placement)
    - `src/components/dashboard/warehouses/use-dashboard-warehouse.tsx` (read-only inventory of inline types, mutations, URL filter, context shape)
    - `src/components/dashboard/devices/use-dashboard-devices.tsx` (read-only: locate duplicate `extractMutationError` for citation text only)
  </read_first>
  <files>.docs/developer/refactors/hooks/dashboard/warehouses/use-dashboard-warehouse.md</files>
  <action>
    Append or extend sections without deleting Phase 18/19 metadata: **Hook Responsibility** filled per template — Current source, Target hook file `src/hooks/dashboard/locations/use-dashboard-warehouse.ts`, Used by (reuse canonical list), fetching/mutation/loading/error/refetch ownership, DTO transformation ownership, Exposes actions (document **target** nested `actions` object with `createWarehouse`, `createZone`, `createBin`, `refresh` per D-20-09/D-20-10 and CFR-12 — describe current flat callbacks as interim only). Add **Inputs**, **Returned DTO** (page-ready fields, not raw API), **Actions** subsection describing target shape, **Dependencies**, **Provider/context decision** (compatibility scaffolding until consumer migration proven per D-20-08). Add full **`## Logic Mapping`** per workflow: **Logic Found** lists all nine CFR-09 categories; **Logic Movement Plan** table uses CFR-10 target kinds — explicitly rows for raw payloads to `src/types/api/locations/` (or shard names per RESEARCH), UI DTOs to `src/types/dto/locations/`, mappings to `src/lib/transformers/locations`, mutations + refresh under hook `actions`, duplicate **`extractMutationError`** to **`src/lib/api/extract-mutation-error.ts`** as **utility** (CFR-13; **not** transformer). **Reason/Risk** columns per D-20-03. Cite **evidence**: duplicate helper exists in `src/components/dashboard/devices/use-dashboard-devices.tsx` (file path + function name in Notes only). **New Files Needed** lists planned modules (Phase 22). **Notes**: provider compatibility checklist — URL `warehouseId` filter equivalence, error dialog titles/sources unchanged, consumer inventory from Used by, dual-export migration option called out without prescribing production code now. Fix any dismount/navigation table row that incorrectly labels mutation-error helper under `src/lib/transformers` — align narrative to `src/lib/api/` per CFR-13. Do not change meanings of API payloads or mutation outcomes.
  </action>
  <verify>
    <automated>rg -n 'Logic Mapping|extract-mutation-error|actions\.|src/types/api|src/lib/transformers/locations' .docs/developer/refactors/hooks/dashboard/warehouses/use-dashboard-warehouse.md; rg -q 'use-dashboard-devices' .docs/developer/refactors/hooks/dashboard/warehouses/use-dashboard-warehouse.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    File contains `## Logic Mapping`; movement table includes utility row for `extract-mutation-error.ts`; `use-dashboard-devices` cited as duplicate evidence; no `src/**` diff.
  </acceptance_criteria>
  <done>Canonical hook doc is Phase-22-ready for warehouse split.</done>
</task>

<task type="auto">
  <name>Task 2: Component-hook parity + layout integration cross-link</name>
  <read_first>
    - `.docs/developer/refactors/hooks/dashboard/warehouses/use-dashboard-warehouse.md` (post Task 1)
    - `.docs/developer/refactors/components/hook/dashboard/warehouses/use-dashboard-warehouse.md`
    - `.docs/developer/refactors/components/_refactor-map.md` (layout integration row)
  </read_first>
  <files>
    .docs/developer/refactors/components/hook/dashboard/warehouses/use-dashboard-warehouse.md
    .docs/developer/refactors/components/_refactor-map.md
  </files>
  <action>
    Sync **generated** component-hook doc so Logic Mapping / hook responsibility summaries **do not contradict** canonical doc — prefer short pointer blocks (“see canonical”) plus duplicated **movement table** only if needed for standalone reading; keep CFR-09 coverage either inline or by reference. In `_refactor-map.md`, under layout/`DashboardWarehouseProvider` integration notes, add **cross-link** sentence pointing to warehouse hook Logic Mapping for provider responsibilities (no new heavy movement table per `20-RESEARCH.md`). Preserve existing Phase 18 table rows.
  </action>
  <verify>
    <automated>rg -n 'Logic Mapping|canonical|hooks/dashboard/warehouses/use-dashboard-warehouse' .docs/developer/refactors/components/hook/dashboard/warehouses/use-dashboard-warehouse.md; rg -n 'Logic Mapping|warehouse' .docs/developer/refactors/components/_refactor-map.md | head -20; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    Component-hook doc references canonical path or repeats movement essentials; `_refactor-map.md` links warehouse provider narrative to hook Logic Mapping; src diff empty.
  </acceptance_criteria>
  <done>Warehouse documentation cluster consistent across canonical, generated, refactor map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ Phase 22 implementer | Wrong tier for mutation-error helper misroutes code to transformers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | T | warehouse markdown | mitigate | Explicit utility path `src/lib/api/extract-mutation-error.ts`; cite devices duplicate. |
| T-20-02-02 | T | `src/**` | mitigate | Verify commands require empty git diff for `src`. |
</threat_model>

<verification>
`git diff --name-only -- 'src/**'` empty after plan; spot-check movement table includes hook + transformer + utility rows.
</verification>

<success_criteria>
Warehouse hook docs implementation-ready; CFR-13 satisfied with devices evidence line; refactor map cross-linked.
</success_criteria>

<output>
After completion, create `.planning/phases/20-logic-mapping-and-hook-decisions/20-02-SUMMARY.md`.
</output>
