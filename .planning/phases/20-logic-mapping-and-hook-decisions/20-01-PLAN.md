---
phase: 20-logic-mapping-and-hook-decisions
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .docs/developer/refactors/_logic-mapping-summary.md
autonomous: true
requirements:
  - CFR-09
  - CFR-10
must_haves:
  truths:
    - Phase 20 executor opens one summary file describing full vs concise Logic Mapping depth rules and CFR-10 target-kind vocabulary.
    - Refactor tree remains free of `src/**` edits after this plan.
  artifacts:
    - path: .docs/developer/refactors/_logic-mapping-summary.md
      provides: Phase 20 taxonomy, depth rule, verification commands, cross-links to workflow rule
      contains: Logic Mapping
  key_links:
    - from: _logic-mapping-summary.md
      to: .cursor/rules/refactor-documentation-workflow.mdc
      via: explicit path reference in doc body
---

<objective>
Create the Phase 20 scaffold document that anchors Logic Mapping depth (full vs concise), CFR-10 movement vocabulary, and grep-based verification gates so later plans execute uniformly.

Purpose: Single source for executors applying D-20-01 through D-20-03 before touching per-component markdown.

Output: New `_logic-mapping-summary.md` under refactor docs root.
</objective>

<execution_context>
@/Users/michele/Dev/Projects/NextJS/lemon-wms/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/20-logic-mapping-and-hook-decisions/20-CONTEXT.md
@.planning/phases/20-logic-mapping-and-hook-decisions/20-RESEARCH.md
@.cursor/rules/refactor-documentation-workflow.mdc
@.docs/developer/refactors/components/_classification-summary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Phase 20 logic-mapping summary scaffold</name>
  <read_first>
    - `.planning/phases/20-logic-mapping-and-hook-decisions/20-CONTEXT.md` (decisions D-20-01..D-10)
    - `.planning/phases/20-logic-mapping-and-hook-decisions/20-RESEARCH.md` (Q1 depth rule, CFR-10 taxonomy)
    - `.cursor/rules/refactor-documentation-workflow.mdc` (`## Logic Mapping` template headings)
    - `.docs/developer/refactors/components/_classification-summary.md` (split / split-or-grouped / high / multi-component columns)
  </read_first>
  <files>.docs/developer/refactors/_logic-mapping-summary.md</files>
  <action>
    Create `.docs/developer/refactors/_logic-mapping-summary.md` with: (1) purpose and Phase 20 boundary (documentation-only; no `src/**` edits; no new target dirs per Phase 22 ownership); (2) **Depth rule** verbatim intent from `20-RESEARCH.md` Q1 — **full** `## Logic Mapping` with `### Logic Found`, `### Logic Movement Plan`, `### New Files Needed`, `### Notes` when `_classification-summary.md` row has Action `split` or `split or keep grouped`, Risk `high`, or Multi-component `yes`; **concise** subsection for move-only, single-component, low/medium risk, no split, no multi-component; (3) **CFR-10 target kinds** table: hook, transformer, types/api, types/dto, utility, retained render, deferred — with one-line examples referencing warehouse slice; (4) **Verification** subsection listing exact commands from tasks (empty `src` diff; `rg '^## Logic Mapping'` spot-check); (5) pointer to hook template fields (CFR-11/12) and shared mutation-error utility path `src/lib/api/extract-mutation-error.ts` (CFR-13, Phase 22 implementation); (6) integration row for `layout.tsx` documented in `_refactor-map.md` — cross-link only, no heavy table per `20-RESEARCH.md` Q1. Preserve behavior: document current vs planned targets only; never imply API/DTO/mutation semantic changes.
  </action>
  <verify>
    <automated>test -f .docs/developer/refactors/_logic-mapping-summary.md; rg -n 'Logic Mapping|CFR-10|full|concise|extract-mutation-error' .docs/developer/refactors/_logic-mapping-summary.md; test -z "$(git diff --name-only -- 'src/**')"</automated>
  </verify>
  <acceptance_criteria>
    `rg -c '^## Logic Mapping' .docs/developer/refactors/_logic-mapping-summary.md` is ≥ 0 (document may use variant headings under summary — at minimum `Logic Mapping` appears as anchor text); `git diff --name-only -- 'src/**'` prints nothing.
  </acceptance_criteria>
  <done>Scaffold file exists, encodes depth rule and CFR-10 vocabulary, references workflow template path.</done>
</task>

<task type="auto">
  <name>Task 2: Baseline verification gates (no src drift)</name>
  <read_first>
    - `.docs/developer/refactors/_logic-mapping-summary.md` (verification subsection just written)
  </read_first>
  <files>.docs/developer/refactors/_logic-mapping-summary.md</files>
  <action>
    Run documented gates locally: confirm `git diff --name-only -- 'src/**'` is empty; `git status --porcelain -- src` empty; if `src/components/features/locations` already exists on branch, **do not** run `test ! -d`; if absent, optionally record `test ! -d src/components/features/locations` pass in summary footnote as Phase 20 baseline. Do not create forbidden dirs.
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only -- 'src/**')"; test -z "$(git status --porcelain -- src 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    Exit code 0 for empty src diff and empty src porcelain; no new directories under `src/components/features` created.
  </acceptance_criteria>
  <done>Baseline recorded; executor-ready gates reproducible from summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → repo | Markdown edits must never alter runtime code paths under `src/**`. |
| doc drift | Wrong taxonomy causes Phase 22 to implement incorrect layer placement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | T | PLAN executor | mitigate | Task verify: `git diff --name-only -- 'src/**'` must be empty after edits. |
| T-20-01-02 | I | `_logic-mapping-summary.md` | mitigate | Grep for required section anchors; cite `_classification-summary.md` for depth rule. |
</threat_model>

<verification>
After wave: `rg '^## Logic Mapping' .docs/developer/refactors/_logic-mapping-summary.md` optional; `git diff --name-only -- 'src/**'` empty.
</verification>

<success_criteria>
Summary file exists; depth rule and CFR-10 vocabulary explicit; src tree untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/20-logic-mapping-and-hook-decisions/20-01-SUMMARY.md` per execute workflow.
</output>
