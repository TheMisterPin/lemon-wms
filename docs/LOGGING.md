# Logging roadmap (Phase 10)

Lemon WMS logging will use three append-only models:
- `UserActivityEntry` (UAE)
- `BinOperationEntry` (BOE)
- `ItemLedgerEntry` (ILE)

Phase 10 will add UI/API viewers and reporting across these entries.
