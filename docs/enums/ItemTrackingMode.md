# ItemTrackingMode

## Purpose
Defines item-level inventory tracking strategy.

## Values
| Value | Meaning |
|---|---|
| `NONE` | Allowed `ItemTrackingMode` value used by business rules. |
| `LOT` | Allowed `ItemTrackingMode` value used by business rules. |
| `SERIAL` | Allowed `ItemTrackingMode` value used by business rules. |
| `FIFO` | Allowed `ItemTrackingMode` value used by business rules. |
