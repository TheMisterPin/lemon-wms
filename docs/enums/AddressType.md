# AddressType

## Purpose
Defines semantic purpose of an address record.

## Values
| Value | Meaning |
|---|---|
| `PRIMARY` | Allowed `AddressType` value used by business rules. |
| `BILLING` | Allowed `AddressType` value used by business rules. |
| `SHIPPING` | Allowed `AddressType` value used by business rules. |
| `RETURNS` | Allowed `AddressType` value used by business rules. |
| `WAREHOUSE` | Allowed `AddressType` value used by business rules. |
| `HQ` | Allowed `AddressType` value used by business rules. |
