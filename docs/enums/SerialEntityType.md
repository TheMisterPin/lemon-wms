# SerialEntityType

## Purpose
Defines entity kinds a serial configuration/record can target.

## Values
| Value | Meaning |
|---|---|
| `ITEM` | Allowed `SerialEntityType` value used by business rules. |
| `BOX` | Allowed `SerialEntityType` value used by business rules. |
| `BOX_LINE` | Allowed `SerialEntityType` value used by business rules. |
| `PALLET` | Allowed `SerialEntityType` value used by business rules. |
| `ORDER` | Allowed `SerialEntityType` value used by business rules. |
| `ORDER_LINE` | Allowed `SerialEntityType` value used by business rules. |
| `USER` | Allowed `SerialEntityType` value used by business rules. |
