# LotStatus

## Purpose
Represents lifecycle state of a lot.

## Values
| Value | Meaning |
|---|---|
| `ACTIVE` | Allowed `LotStatus` value used by business rules. |
| `QUARANTINE` | Allowed `LotStatus` value used by business rules. |
| `EXPIRED` | Allowed `LotStatus` value used by business rules. |
| `CONSUMED` | Allowed `LotStatus` value used by business rules. |
