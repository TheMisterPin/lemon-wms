# OrderType

## Purpose
Represents the operational order family.

## Values
| Value | Meaning |
|---|---|
| `PURCHASE` | Allowed `OrderType` value used by business rules. |
| `SALES` | Allowed `OrderType` value used by business rules. |
| `TRANSFER` | Allowed `OrderType` value used by business rules. |
| `RETURN` | Allowed `OrderType` value used by business rules. |
| `ADJUSTMENT` | Allowed `OrderType` value used by business rules. |
