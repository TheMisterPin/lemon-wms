# OrderStatus

## Purpose
Represents lifecycle state for operational orders.

## Values
| Value | Meaning |
|---|---|
| `DRAFT` | Allowed `OrderStatus` value used by business rules. |
| `CONFIRMED` | Allowed `OrderStatus` value used by business rules. |
| `RELEASED` | Allowed `OrderStatus` value used by business rules. |
| `EXECUTING` | Allowed `OrderStatus` value used by business rules. |
| `PAUSED` | Allowed `OrderStatus` value used by business rules. |
| `EXECUTED` | Allowed `OrderStatus` value used by business rules. |
| `EXECUTED_WITH_PROBLEMS` | Allowed `OrderStatus` value used by business rules. |
| `SIGNED_OFF` | Allowed `OrderStatus` value used by business rules. |
| `CANCELLED` | Allowed `OrderStatus` value used by business rules. |
