# Role

## Purpose
Defines authorization roles for users.

## Values
| Value | Meaning |
|---|---|
| `OWNER` | Allowed `Role` value used by business rules. |
| `OFFICE_MANAGER` | Allowed `Role` value used by business rules. |
| `OFFICE_WORKER` | Allowed `Role` value used by business rules. |
| `WAREHOUSE_MANAGER` | Allowed `Role` value used by business rules. |
| `WAREHOUSE_WORKER` | Allowed `Role` value used by business rules. |
