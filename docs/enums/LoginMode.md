# LoginMode

## Purpose
Defines how devices/users authenticate during login.

## Values
| Value | Meaning |
|---|---|
| `PIN` | Allowed `LoginMode` value used by business rules. |
| `AUTOMATIC` | Allowed `LoginMode` value used by business rules. |
