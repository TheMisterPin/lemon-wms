# SerialMode

## Purpose
Defines serial generation behavior.

## Values
| Value | Meaning |
|---|---|
| `INCREMENTAL` | Allowed `SerialMode` value used by business rules. |
| `PARTIAL` | Allowed `SerialMode` value used by business rules. |
