export default function WarehouseHomePage() {
  return (
    <main className="border border-terminal-border bg-terminal-surface p-6">
      <p className="font-[family-name:var(--font-terminal-label)] text-[0.6rem] uppercase tracking-[0.2em] text-terminal-text-dim">
        LEMON WMS // ORDER MANAGEMENT
      </p>
      <h1 className="mt-1 font-[family-name:var(--font-terminal)] text-[1.1rem] font-bold uppercase tracking-[0.05em] text-terminal-accent">
        ORDER POOL
      </h1>
      <p className="mt-4 font-[family-name:var(--font-terminal-label)] text-[0.6rem] uppercase tracking-[0.15em] text-terminal-text-dim">
        PHASE 0 — WAREHOUSE SHELL COMPLETE
      </p>
    </main>
  )
}
